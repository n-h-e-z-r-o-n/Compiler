

int addition(int a, ){

}
int subtraction(int a, int b){

}

/*

     if token[current_token + 1][0] == 'COdMMA':
                        if token[current_token + 2][0] != 'RIGHT_PAREN':
                            parame +=  token[current_token + 1][1] + ' '
                            current_token += 1
                        else:
                            parame += "  <error-remove ','> "
                            print("syntax error: ',' error in parameters  at line", token[current_token + 2][2])
                            current_token += 1
                    elif token[current_token + 1][0] == 'KEYWORD' or token[current_token + 1][0] == 'IDENTIFIER':
                        parame += "  <missing ','>  " + token[current_token + 1][1] + ' '
                        current_token += 1
                    else:
                        if token[current_token + 1][0] == 'RIGHT_PAREN':
                            parame += "  <remove ','>  "
                     else:
                    print("Error Expected IDENTIFIER")

*/